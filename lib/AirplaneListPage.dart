import 'package:flutter/material.dart';

class AirplaneListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Airplane List Page'),
      ),
      body: Center(
        child: const Text('This is the Airplane List Page'),
      ),
    );
  }
}